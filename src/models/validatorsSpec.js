/* eslint-env mocha */
/* eslint no-unused-vars: "off" */

// var should = require('should');

// var validators = require('./validators');

// describe('durationValidator', function() {
//   it('should return true for valid period/duration combinations', function(done) {
//     validators.durationValidator([1, 2], 1).should.equal(true);
//     validators.durationValidator([3], 1).should.equal(true);
//     validators.durationValidator([1], 1).should.equal(true);
//     validators.durationValidator([2, 3], 2).should.equal(true);
//     validators.durationValidator([1, 2, 3], 3).should.equal(true);
//     done();
//   });

//   it('should return false for invalid period/duration combinations', function(done) {
//     validators.durationValidator([1, 2, 3], 2).should.equal(false);
//     validators.durationValidator([1, 2], 2).should.equal(false);
//     validators.durationValidator([1, 3], 2).should.equal(false);
//     validators.durationValidator([1, 3], 3).should.equal(false);
//     validators.durationValidator([1], 2).should.equal(false);
//     done();
//   });
// });
